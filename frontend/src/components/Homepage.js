import React from 'react';

// components
import FrontUI from './FrontUI';

const Homepage = () => {
    return (
        <div className='wrapper-hmpg'>
            <FrontUI />
        </div>
    );
};

export default Homepage;
